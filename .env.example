DATABASE_URL=sqlite:///./todo.db
# Tested against this model - behavior with other models (especially the
# relative-date reasoning in app/ai/extract.py's prompt) isn't guaranteed.
# See README.md for details, and how to point this at a different model.
BEDROCK_MODEL_ID=openai.gpt-oss-120b-1:0
AWS_REGION=
